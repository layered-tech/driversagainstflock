Turn-by-turn instruction banner, pinned to the top of the screen during active navigation.

```jsx
<NavBanner maneuver="right" distance="500 ft" instruction="Turn right onto Market St" then="then left" />
```

Big tabular distance + maneuver arrow in a brand-green tile. Keep instruction copy calm and literal — this is safety-critical, no jokes here.
