Pill text field for search and forms.

```jsx
<Input tone="glass" placeholder="Where to?" leadingIcon={<SearchIcon/>} />
<Input tone="light" placeholder="Email" size="lg" />
```

`tone="glass"` (default) for floating-over-map search; `tone="light"` on marketing/forms. Pair with the SearchBar component for the full map search affordance.
