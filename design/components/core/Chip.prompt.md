Selectable filter / quick-action pill. Rows of these scroll horizontally above the map.

```jsx
<Chip icon={<CamIcon/>} selected>Cameras</Chip>
<Chip icon={<GasIcon/>}>Gas</Chip>
```

Selected = Signal Green fill. `tone="light"` for use on marketing/forms.
