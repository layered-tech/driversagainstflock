Generic content container.

```jsx
<Card tone="light" interactive>…</Card>
<Card tone="glass" padding="var(--space-5)">…</Card>
```

`light` = white with soft shadow (marketing/docs). `glass` = frosted dark surface that stays readable over any map. Radius 18px. For route choices use the purpose-built RouteCard instead.
